.DS_Store
.swp
__pycache__
.agent/

# Ignore IDE settings by default to respect other users' preferences.
# Force stage settings if you want to share them.
.vscode/settings.json
.antigravity/settings.json
.zed/settings.json

# Specific ignores can be added to the project's .git/info/excludes file
# The .git folder is never commited so this remains a local only config.
