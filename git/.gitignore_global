.DS_Store
.swp
__pycache__
.agent/

# Ignore VSCode settings by default to respect other users' preferences.
# Force stage settings if you want to share them.
.vscode/settings.json
.antigravity/settings.json

# Ignore Drupal config files for local development.
# Allows custom config without interferring with the team's codebase.
config/local/
